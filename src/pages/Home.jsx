import React from 'react';
import { Container, Typography } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Typography variant="h3">Welcome to the Recruitment Tracker</Typography>
      <Typography variant="body1">
        Manage job postings and track candidate applications efficiently.
      </Typography>
    </Container>
  );
};

export default Home;
